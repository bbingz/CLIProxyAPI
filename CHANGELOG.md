# Changelog

## 2026-06-28

- Completed direct Volcengine Ark API comparison and one Claude review pass for the Command Code strict-response work. Direct `https://ark.cn-beijing.volces.com/api/coding/v1/chat/completions` `glm-5.2` responses returned `reasoning_content` in both non-streaming `message` and streaming `delta`, while direct `10.0.8.9:8317` Command Code responses returned only standard `content`/`role` message and delta fields and preserved reasoning counts in `completion_tokens_details.reasoning_tokens`; both providers returned HTTP 200 and valid usage totals. Claude review found no blocking findings for commit `6e892d19`; source adjudication confirmed production streaming uses one persistent converter and non-streaming finish reasons are normalized through `mapFinishReason`.
- Normalized Command Code OpenAI response fields end to end: generated per-request `chatcmpl-commandcode-*` IDs, removed non-standard `reasoning_content` from non-streaming and streaming OpenAI output, emitted a standard assistant `role` stream chunk, converted `tool-input-*` and fallback `tool-call` events into OpenAI `delta.tool_calls`, deduped duplicate tool terminal events while preferring full `tool-call.input`, and mapped reasoning counts into `completion_tokens_details.reasoning_tokens`. Deployed plugin sha256 `4ab10669b86f75d3cd0346659c9e3e5cd37328c25dd5746474030776e7d591bb` to `10.0.8.9`, active PID `32493`, backup `/Users/bing/-Tools-/CLIProxyAPI/deploy-backups/20260628-201000-strict-openai-response-v2`. Verified direct `8317` `glm-5.2` non-streaming and streaming responses against a field-level checker: HTTP 200, `chat.completion`/`chat.completion.chunk`, no `reasoning_content`, one stream id, assistant role chunk, one finish chunk, one usage chunk, `[DONE]`, no malformed SSE, and `total_tokens = prompt_tokens + completion_tokens`. Compared against New API's active Volcengine GLM route: New API Channel #3 is enabled for `glm-5.2`; Channel #10 CommandCode remains disabled by user choice; Volcengine itself emits `reasoning_content`, so it is a compatibility reference rather than a strict OpenAI-field baseline.
- Fixed Command Code streaming usage accounting by caching `finish-step` usage, emitting exactly one terminal finish chunk, then emitting a final OpenAI-compatible `choices: []` usage chunk before `[DONE]`; normalized `usage.total_tokens` to `prompt_tokens + completion_tokens` and added `prompt_tokens_details.cached_tokens` when present. Verified on `10.0.8.9` that `glm-5.2` direct non-streaming and streaming responses both include usage with non-zero completion tokens, one finish chunk, one usage chunk, and no malformed SSE lines. New API historical logs showed Channel #10 `glm-5.2` streaming rows with `completion_tokens=0` before this fix; Channel #10 currently has `status=2`, so New API routing may require re-enabling that channel before split tests include Command Code again.
- Generalized Command Code short aliases for every canonical provider model by exposing the lower-case model basename, for example `deepseek-v4-flash`, `kimi-k2.7-code`, `minimax-m3`, `qwen3.7-max`, `step-3.7-flash`, and `nemotron-3-ultra-550b-a55b`; verified on `10.0.8.9` that all 12 aliases are present, `commandcode` model count is 24, `glm-5.2` and `deepseek-v4-flash` both pass non-streaming and streaming smoke tests, and Grok/Gemini counts remain 12/8.
- Added Command Code short model aliases `glm-5.2` and `glm-5.1` while normalizing upstream requests to `zai-org/GLM-5.2` and `zai-org/GLM-5.1`; verified on `10.0.8.9` that `/v1/models` exposes both short aliases, `glm-5.2` non-streaming returns HTTP 200/content `OK`, and `glm-5.2` streaming returns HTTP 200/content `OK`/3 chunks/0 malformed chunks.
- Added a Command Code Go plugin with OAuth callback support, model registration, OpenAI chat-completion request translation, and Command Code `/alpha/generate` response translation.
- Deployed the branch build to `10.0.8.9` with the plugin enabled under `/Users/bing/-Tools-/CLIProxyAPI/plugins/commandcode.dylib` and Command Code auth stored at `/Users/bing/.cli-proxy-api/commandcode.json`.
- Fixed Command Code streaming output so plugin chunks are raw OpenAI chat-completion JSON payloads; CLIProxyAPI's OpenAI handler owns SSE `data:` framing and terminal `[DONE]`.
- Verified on `10.0.8.9`: `/v1/models` HTTP 200 with 24 `owned_by=commandcode` models after adding short aliases; Grok count 12; Gemini count 8; Command Code non-streaming smoke HTTP 200/content `OK`; Command Code streaming smoke HTTP 200/content `OK`/15 chunks/0 malformed chunks; `zai-org/GLM-5.2` non-streaming smoke HTTP 200/content `OK`.
